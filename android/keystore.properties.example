# Copy this file to android/keystore.properties and fill the values before building a signed AAB
# Do NOT commit the real android/keystore.properties file to source control.
storeFile=keystore.jks
storePassword=CHANGE_ME
keyAlias=my-key-alias
keyPassword=CHANGE_ME
